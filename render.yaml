# Render Blueprint for Avalanche Bridge API
services:
  - type: web
    name: avalanche-bridge-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: CORS_ORIGIN
        sync: false
      - key: NODE_ENV
        value: production
    plan: free
